export const validate =(input)=>{
    
        const errors = {};

        // if (!values.name) {
        //   errors.name = 'Requerido';
        // } else if (
        //   ! /^.+@.+\..+$/i.test(values.name)
        // ) {
        // errors.name = 'Caracteres invalidos para el nombre';
        // }

        // if (!values.email) {
        //   errors.email = 'Requerido';
        // } else if (
        //   !/^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i.test(values.email)
        // ) {
        //   errors.email = 'Correo electrónico inválido';
        // }

        // if (!values.birthdate) {
        //   errors.birthdate = 'Requerido';
        // } else if (
        //   !/^(0[1-9]|[12][0-9]|3[01])\/(0[1-9]|1[0-2])\/(19|20)\d{2}$/.test(values.birthdate)            
        // ) {
        //   errors.birthdate = 'Fecha inválida';
        // }
        // if (!values.nDni) {
        //   errors.nDni = 'Requerido';
        // } else if (
        //   ! /^.+@.+\..+$/i.test(values.nDni)
        // ) {
        // errors.nDni = 'Caracteres invalidos para el nDni';
        // }
        // if (!values.username) {
        //   errors.username = 'Requerido';
        // } else if (
        //   ! /^.+@.+\..+$/i.test(values.username)
        // ) {
        // errors.username = 'Caracteres invalidos para el usario';
        // }

        // if (!values.password) {
        //   errors.password = 'Requerido';
        // } else if (values.password.length < 8) {
        //   errors.password = 'La contraseña debe tener al menos 8 caracteres';
        // }
        return errors;

};